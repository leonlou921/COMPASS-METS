[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "brats-mets-n03"
version = "1.0.0"
description = "Training and frozen inference pipeline for the BraTS-METS N03 candidate"
readme = "README.md"
requires-python = ">=3.10,<3.12"
license = {text = "Apache-2.0"}
dependencies = [
  "joblib==1.5.1",
  "lightgbm==4.6.0",
  "nibabel==5.3.3",
  "numpy==1.26.4",
  "pandas==2.3.0",
  "scikit-learn==1.7.0",
  "scipy==1.13.1",
  "SimpleITK==2.5.3",
]

[project.optional-dependencies]
test = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
  ".",
  "inference/src",
  "inference/vendor/lcv1",
  "inference/vendor/portfolio",
  "inference/vendor/pipeline",
]
